[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "roomtrace-processor"
version = "0.3.0"
description = "Fully local Open3D TSDF reconstruction and Blender export tools for RoomTrace captures"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "Pillow>=10.0",
  "open3d>=0.18,<0.20",
]

[project.optional-dependencies]
science = ["scipy>=1.10"]
dev = ["pytest>=8.0"]

[project.scripts]
roomtrace = "roomtrace.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
